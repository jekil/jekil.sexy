language: python
python:
  - "2.7"
sudo: required
cache: pip
branches:
  only:
    - master
install:
  - pip install -r requirements.txt
script:
  - make publish github
before_script:
  - git config --global user.email ${EMAIL}
  - git config --global user.name ${NICK}
